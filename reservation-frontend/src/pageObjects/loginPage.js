import { testIds } from '../constants/testIds';

export const loginPage = {
  root: testIds.login.page,
  form: testIds.login.form,
  usernameInput: testIds.login.usernameInput,
  passwordInput: testIds.login.passwordInput,
  submitButton: testIds.login.submitButton,
  errorMessage: testIds.login.errorMessage
};
